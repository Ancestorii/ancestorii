'use client';

import Particles from '@/components/Particles';

export default function TextStarField() {
  return (
    <div className="absolute inset-0 z-0 pointer-events-none">
      <Particles />
    </div>
  );
}

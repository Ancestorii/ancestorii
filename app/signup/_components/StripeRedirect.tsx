'use client';

export default function StripeRedirect() {
  return (
    <div
      style={{
        padding: 40,
        background: 'red',
        color: 'white',
        fontSize: 24,
        textAlign: 'center',
      }}
    >
      STRIPE REDIRECT COMPONENT IS MOUNTED
    </div>
  );
}
